# Documentation

Project documentation for TM Search Engine lives here — design notes, architecture decisions, and setup guides that don't belong in the top-level [README](../README.md).

Suggested docs to add as the project grows:

- `architecture.md` — how the extraction pipeline, search index, backend API, and frontend fit together
- `data-model.md` — Meilisearch index schema, document fields, and search-relevance tuning notes
- `setup-windows.md` — machine-specific setup notes (Tesseract, ImageMagick/Wand, etc.)

Currently empty — nothing has been written yet beyond the top-level README and inline code comments.
