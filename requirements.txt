# TM Search Engine - Python Dependencies
# Phase 1 MVP Development

# Document Processing
pdfplumber==0.9.0
pytesseract==0.3.10
python-docx==0.8.11
Pillow==10.2.0

# OCR & Image Processing
opencv-python==4.11.0.86  # 4.8.0.74 is built against the NumPy 1.x ABI and
                           # raises "AttributeError: _ARRAY_API not found"
                           # under NumPy 2.x; 4.9.0.80+ added NumPy 2 support
PyMuPDF==1.28.2  # rasterizes PDF pages for OCR fallback (pdfplumber's own
                  # to_image() requires ImageMagick, which is painful on Windows)

# Search & Indexing
meilisearch==0.43.0  # 0.27.0 (originally pinned) predates the pydantic-based
                      # models this project's Meilisearch server (v1.53.x) and
                      # search_index.py's task-handling rely on

# Database
# sqlite3 is part of the Python standard library — no pip install needed

# Backend API
fastapi==0.104.0
uvicorn==0.24.0
pydantic==2.4.0
python-multipart==0.0.6

# Development & Testing
pytest==7.4.0
pytest-cov==4.1.0
black==23.10.0
flake8==6.1.0
httpx==0.27.2  # required by fastapi.testclient.TestClient; pin <0.28 —
                # 0.28 dropped the `app=` shortcut this starlette version needs

# Utilities
python-dotenv==1.0.0
tqdm==4.66.0
requests==2.31.0

# Optional: Performance monitoring
psutil==5.9.0
